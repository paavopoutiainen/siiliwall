type Board {
  id: ID!
  name: String!
  orderNumber: Int!
  columns: [Column!]!
  columnOrder: [ID!]!
  swimlaneOrder: [ID!]!
}

type Query {
  allBoards: [Board!]!
  boardById(id: ID!): Board!
}

type Mutation {
  addBoard(name: String!): Board
}

