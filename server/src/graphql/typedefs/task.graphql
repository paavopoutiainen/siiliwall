type Task {
  id: ID!
  title: String!
  content: String
  owner: String
  size: Float
  column: Column!
  columnOrderNumber: Int!
  subtasks: [Subtask!]!
  subtaskOrder: [ID!]!
  deletedAt: String
}

type Query {
  taskById(id: ID!): Task!
}

type Mutation {
  addTaskForColumn(columnId: ID!, title: String!, size: Float, owner: String, content: String ): Task
  editTaskById(id: ID!, title: String!, size: Float, owner: String, content: String): Task
  deleteTaskById(id: ID!): ID
  archiveTaskById(id: ID!): ID
  restoreTaskById(id: ID!): Task
}
